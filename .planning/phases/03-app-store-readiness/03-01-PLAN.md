---
phase: 03-app-store-readiness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - AppStoreAssets/Metadata/en/description.txt
  - AppStoreAssets/Metadata/en/keywords.txt
  - AppStoreAssets/Metadata/en/subtitle.txt
  - AppStoreAssets/Metadata/tr/description.txt
  - AppStoreAssets/Metadata/tr/keywords.txt
  - AppStoreAssets/Metadata/tr/subtitle.txt
  - AppStoreAssets/Metadata/en/promotional_text.txt
  - AppStoreAssets/Metadata/tr/promotional_text.txt
  - AppStoreAssets/Metadata/cpp_config.md
  - HabitLand/Screens/Settings/GeneralSettingsView.swift
autonomous: true
requirements:
  - ASR-02
  - ASR-03
  - ASR-04
  - ASR-05
  - ASR-06

must_haves:
  truths:
    - "ASO-optimized English metadata exists with subtitle <= 30 chars and keywords <= 100 chars"
    - "ASO-optimized Turkish metadata exists with subtitle <= 30 chars and keywords <= 100 chars"
    - "No keyword repetition across title + subtitle + keywords fields"
    - "Privacy policy and terms of use URLs are accessible from the web"
    - "In-app legal views link to web-hosted versions"
    - "App icon renders correctly at 1024x1024 with dark and tinted variants"
    - "3 Custom Product Pages documented with distinct screenshot orders and promotional text"
  artifacts:
    - path: "AppStoreAssets/Metadata/en/description.txt"
      provides: "English App Store description"
    - path: "AppStoreAssets/Metadata/en/keywords.txt"
      provides: "English keyword field (100 chars max)"
    - path: "AppStoreAssets/Metadata/en/subtitle.txt"
      provides: "English subtitle (30 chars max)"
    - path: "AppStoreAssets/Metadata/tr/description.txt"
      provides: "Turkish App Store description"
    - path: "AppStoreAssets/Metadata/tr/keywords.txt"
      provides: "Turkish keyword field (100 chars max)"
    - path: "AppStoreAssets/Metadata/tr/subtitle.txt"
      provides: "Turkish subtitle (30 chars max)"
    - path: "AppStoreAssets/Metadata/cpp_config.md"
      provides: "Custom Product Page configuration"
  key_links:
    - from: "GeneralSettingsView.swift"
      to: "docs/privacy.html"
      via: "URL link in legal section"
      pattern: "Link.*privacy"
    - from: "GeneralSettingsView.swift"
      to: "docs/terms.html"
      via: "URL link in legal section"
      pattern: "Link.*terms"
---

<objective>
Create all App Store listing text content (metadata, keywords, descriptions) in English and Turkish, verify legal URLs and app icon, and document Custom Product Page configurations.

Purpose: Complete the non-screenshot App Store listing requirements so the app is ready for submission.
Output: Metadata text files, CPP config doc, updated in-app legal links, icon verification.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-app-store-readiness/03-CONTEXT.md
@.planning/phases/03-app-store-readiness/03-RESEARCH.md

@HabitLand/Screens/Settings/GeneralSettingsView.swift
@HabitLand/Screens/Premium/LegalView.swift
@docs/privacy.html
@docs/terms.html
@HabitLand/Assets.xcassets/AppIcon.appiconset/Contents.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ASO-optimized metadata files in English and Turkish</name>
  <files>
    AppStoreAssets/Metadata/en/description.txt,
    AppStoreAssets/Metadata/en/keywords.txt,
    AppStoreAssets/Metadata/en/subtitle.txt,
    AppStoreAssets/Metadata/en/promotional_text.txt,
    AppStoreAssets/Metadata/tr/description.txt,
    AppStoreAssets/Metadata/tr/keywords.txt,
    AppStoreAssets/Metadata/tr/subtitle.txt,
    AppStoreAssets/Metadata/tr/promotional_text.txt
  </files>
  <read_first>
    .planning/phases/03-app-store-readiness/03-CONTEXT.md,
    .planning/phases/03-app-store-readiness/03-RESEARCH.md
  </read_first>
  <action>
    Create `AppStoreAssets/Metadata/` directory structure with `en/` and `tr/` subdirectories.

    **English metadata (per D-02, D-03, D-04):**

    `en/subtitle.txt`: "Build habits that stick" (exactly 22 chars -- within 30 char limit, per D-02)

    `en/keywords.txt`: Comma-separated, NO spaces after commas, 100 chars max. Words MUST NOT repeat anything from title ("HabitLand") or subtitle ("Build", "habits", "that", "stick"). Include: streak,routine,goals,wellness,gamification,challenge,reminder,daily,health,sleep,tracker,progress,motivation,level. Count characters carefully -- must be <= 100.

    `en/description.txt`: Follow D-03 structure:
    1. Hook (pain point): "Do you keep starting habits only to abandon them?" style per D-01
    2. Solution: What HabitLand does -- gamified tracking, streaks, social accountability
    3. Features: Bullet list of key features (FREE: 3 habits, streaks, sleep tracking. PRO: unlimited, analytics, challenges)
    4. Social proof: Gamification, compete with friends, leaderboards
    5. CTA: "Download now and build habits that last."

    `en/promotional_text.txt`: Short 170-char promotional text for default listing.

    **Turkish metadata (per D-09, D-10, D-11):**

    `tr/subtitle.txt`: "Aliskanliklrini kalici yap" (per D-02 -- verify <= 30 chars with Turkish chars: "Aliskanliklarina kalici yap" or as specified)

    `tr/keywords.txt`: Turkish market keywords, comma-separated, no spaces, 100 chars max. Must NOT repeat title or subtitle words. Include Turkish terms: aliskanlik,rutin,hedef,saglik,uyku,takip,motivasyon,gunluk,hatirlatici,oyunlastirma,ilerleme,streak,seviye.

    `tr/description.txt`: Full Turkish translation following same D-03 structure with D-01 pain point tone. Use the specific phrases from CONTEXT: "Aliskanliklarini hep yarida mi birakiyorsun?" as the hook.

    `tr/promotional_text.txt`: Turkish promotional text (170 chars).

    **CRITICAL validation before saving each file:**
    - subtitle.txt: count chars, must be <= 30
    - keywords.txt: count chars, must be <= 100, no spaces after commas
    - No word appears in both subtitle and keywords for same language
  </action>
  <verify>
    <automated>
      cd /Users/azc/works/HabitLand && \
      for f in AppStoreAssets/Metadata/en/subtitle.txt AppStoreAssets/Metadata/tr/subtitle.txt; do \
        chars=$(wc -m < "$f" | tr -d ' '); \
        echo "$f: $chars chars (max 30)"; \
        [ "$chars" -le 30 ] || echo "FAIL: subtitle too long"; \
      done && \
      for f in AppStoreAssets/Metadata/en/keywords.txt AppStoreAssets/Metadata/tr/keywords.txt; do \
        chars=$(wc -m < "$f" | tr -d ' '); \
        echo "$f: $chars chars (max 100)"; \
        [ "$chars" -le 100 ] || echo "FAIL: keywords too long"; \
        grep -q ", " "$f" && echo "FAIL: spaces after commas in $f" || echo "OK: no spaces after commas"; \
      done && \
      echo "All metadata files validated"
    </automated>
  </verify>
  <acceptance_criteria>
    - 8 metadata files exist in AppStoreAssets/Metadata/{en,tr}/
    - English subtitle exactly "Build habits that stick" (<= 30 chars)
    - Turkish subtitle per D-02 (<= 30 chars)
    - Both keyword files <= 100 chars with no spaces after commas
    - No keyword repetition between title+subtitle and keyword fields
    - Descriptions follow D-03 hook->solution->features->social->CTA structure
    - Pain point messaging tone per D-01
  </acceptance_criteria>
  <done>All 8 metadata text files created and validated for character limits</done>
</task>

<task type="auto">
  <name>Task 2: Verify legal URLs, update in-app links, verify icon, create CPP config</name>
  <files>
    HabitLand/Screens/Settings/GeneralSettingsView.swift,
    AppStoreAssets/Metadata/cpp_config.md
  </files>
  <read_first>
    docs/privacy.html,
    docs/terms.html,
    HabitLand/Screens/Settings/GeneralSettingsView.swift,
    HabitLand/Assets.xcassets/AppIcon.appiconset/Contents.json
  </read_first>
  <action>
    **Legal URLs (ASR-03, per D-14, D-15, D-16):**
    - The `docs/` directory already contains `privacy.html` and `terms.html` with complete content matching LegalView.swift. These are ready for GitHub Pages hosting.
    - Update `GeneralSettingsView.swift` Legal section: add "View Online" links that open Safari to the GitHub Pages URLs. Use `Link("View Online", destination: URL(...)!)` or a button with `UIApplication.shared.open()`. The exact URL will be `https://{username}.github.io/HabitLand/privacy` and `https://{username}.github.io/HabitLand/terms` -- use a constant at the top of the file so the user can easily update the username. Add the links as secondary text or small buttons below the existing "Privacy Policy" and "Terms of Use" rows in the Legal section.
    - Per Pitfall 6 from RESEARCH.md: in-app views MUST reference web URLs.

    **App Icon (ASR-04, per D-17):**
    - Verify `AppIcon.appiconset/Contents.json` has all 3 variants: AppIcon.png (1024x1024), AppIcon-Dark.png, AppIcon-Tinted.png. Already confirmed -- modern Xcode auto-generates all required sizes from the 1024x1024 master. Log verification result.

    **Custom Product Pages (ASR-06, per D-12, D-13):**
    - Create `AppStoreAssets/Metadata/cpp_config.md` documenting the 3 CPPs:

    1. **Fitness CPP:**
       - Promotional text (170 chars max): Fitness-focused messaging about HealthKit steps, exercise habits, leaderboard competition
       - Screenshot order: 01_home_dashboard, 02_streaks_habits, 04_social_leaderboard, 03_sleep_tracking, 05_achievements_xp, 06_premium_pro
       - Subtitle override: "Track your fitness habits" (per D-12)

    2. **Productivity CPP:**
       - Promotional text: Productivity-focused about building routines, streaks, achievements
       - Screenshot order: 02_streaks_habits, 01_home_dashboard, 05_achievements_xp, 04_social_leaderboard, 03_sleep_tracking, 06_premium_pro
       - Subtitle override: "Build productive routines" (per D-12)

    3. **Sleep CPP:**
       - Promotional text: Sleep-focused about tracking rest, sleep quality, better habits
       - Screenshot order: 03_sleep_tracking, 01_home_dashboard, 02_streaks_habits, 04_social_leaderboard, 05_achievements_xp, 06_premium_pro
       - Subtitle override: "Better sleep, better life" (per D-12)

    Per Pitfall 5: each CPP MUST have unique promotional text, not just reordered screenshots.
    Per D-13: same 6 screenshots, different order -- no extra screenshot creation needed.
  </action>
  <verify>
    <automated>
      cd /Users/azc/works/HabitLand && \
      test -f docs/privacy.html && echo "OK: privacy.html exists" || echo "FAIL: missing privacy.html" && \
      test -f docs/terms.html && echo "OK: terms.html exists" || echo "FAIL: missing terms.html" && \
      test -f HabitLand/Assets.xcassets/AppIcon.appiconset/AppIcon.png && echo "OK: AppIcon.png exists" || echo "FAIL: missing AppIcon" && \
      test -f AppStoreAssets/Metadata/cpp_config.md && echo "OK: CPP config exists" || echo "FAIL: missing CPP config" && \
      grep -q "privacy\|terms\|github\|View Online\|view online" HabitLand/Screens/Settings/GeneralSettingsView.swift && echo "OK: legal links in settings" || echo "FAIL: no legal links in settings" && \
      xcodebuild build -scheme HabitLand -destination 'platform=iOS Simulator,name=iPhone 16 Pro Max' -quiet 2>&1 | tail -5
    </automated>
  </verify>
  <acceptance_criteria>
    - docs/privacy.html and docs/terms.html exist (already present) with complete content
    - GeneralSettingsView.swift Legal section includes web URL links for both privacy and terms
    - App builds successfully after settings change
    - AppIcon.appiconset has AppIcon.png (1024x1024) + dark + tinted variants confirmed
    - cpp_config.md documents all 3 CPPs with unique promotional text, screenshot orders, and subtitle overrides per D-12
  </acceptance_criteria>
  <done>Legal URLs verified and linked in-app, icon verified, CPP configuration documented</done>
</task>

</tasks>

<verification>
- All metadata files exist and pass character limit validation
- No keyword repetition across title+subtitle+keywords
- In-app legal section links to web URLs
- App builds without errors
- CPP config covers all 3 audience segments with unique promotional text
- App icon has all required variants
</verification>

<success_criteria>
- 8 metadata text files in AppStoreAssets/Metadata/{en,tr}/ with correct character limits
- GeneralSettingsView shows "View Online" links for privacy and terms
- cpp_config.md documents Fitness, Productivity, and Sleep CPPs
- App icon verified at 1024x1024 with dark and tinted variants
- App compiles successfully
</success_criteria>

<output>
After completion, create `.planning/phases/03-app-store-readiness/03-01-SUMMARY.md`
</output>
