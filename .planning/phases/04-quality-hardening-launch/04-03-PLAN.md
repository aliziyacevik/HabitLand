---
phase: 04-quality-hardening-launch
plan: 03
type: execute
wave: 2
depends_on:
  - 04-01
  - 04-02
files_modified: []
autonomous: false
requirements:
  - QAL-04
  - QAL-06

must_haves:
  truths:
    - "Free tier user can complete onboarding and create 3 habits without hitting bugs"
    - "4th habit creation triggers paywall correctly"
    - "Pro-gated screens show upsell/gate, not broken UI"
    - "App launches in under 1.5 seconds cold start"
    - "Habit list and leaderboard scroll at 60fps"
    - "Memory stays under 100MB during normal usage"
  artifacts: []
  key_links:
    - from: "ProManager.isPro"
      to: "All premium gate checks"
      via: "Returns false for free tier (no debug bypass in release)"
      pattern: "proManager\\.isPro"
---

<objective>
Verify free tier experience end-to-end and validate performance meets defined thresholds.

Purpose: Ensure the app works correctly from a free user's perspective (D-07, D-08, D-09) and meets performance targets (D-16, D-17, D-18). This is the final validation gate before App Store submission.
Output: Verified free tier flows + performance validation + human sign-off.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-quality-hardening-launch/04-RESEARCH.md
@.planning/phases/04-quality-hardening-launch/04-01-SUMMARY.md
@.planning/phases/04-quality-hardening-launch/04-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Release configuration and run automated quality checks</name>
  <read_first>
    - HabitLand/Services/ProManager.swift
    - HabitLand/Services/SharedModelContainer.swift
    - HabitLand/Services/HLLogger.swift
  </read_first>
  <files></files>
  <action>
    Run comprehensive automated verification per D-07, D-19:

    1. **Release build verification (QAL-01 final check):**
       ```bash
       xcodebuild build -scheme HabitLand -destination 'platform=iOS Simulator,name=iPhone 16' -configuration Release 2>&1 | tail -10
       ```
       Must succeed with zero warnings related to screenshotMode.

    2. **Print statement audit (QAL-03 final check):**
       ```bash
       grep -rn "print(" HabitLand/ --include="*.swift" | grep -v "//.*print"
       ```
       Must return 0 results.

    3. **Crash path audit (QAL-02 final check):**
       ```bash
       grep -rn "fatalError\|try!" HabitLand/ --include="*.swift"
       ```
       Only acceptable `try!` is inside SharedModelContainer.swift's final do/catch fallback. Zero `fatalError()` calls.

    4. **screenshotMode audit:**
       ```bash
       grep -rn "screenshotMode" HabitLand/ --include="*.swift"
       ```
       Every result must be inside a `#if DEBUG` block.

    5. **Sheet transition coverage audit:**
       ```bash
       # Count sheet sites vs hlSheetContent usages
       grep -rn "\.sheet(\|\.fullScreenCover(" HabitLand/ --include="*.swift" | wc -l
       grep -rn "hlSheetContent" HabitLand/ --include="*.swift" | grep -v "Effects.swift\|func hlSheet" | wc -l
       ```
       Both counts should match.

    6. **Run existing test suite:**
       ```bash
       xcodebuild test -scheme HabitLand -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing HabitLandTests 2>&1 | tail -20
       ```
       All existing tests must pass.

    Report all findings clearly. If any check fails, fix before proceeding to checkpoint.
  </action>
  <verify>
    <automated>cd /Users/azc/works/HabitLand && xcodebuild build -scheme HabitLand -destination 'platform=iOS Simulator,name=iPhone 16' -configuration Release 2>&1 | grep -E "BUILD|error:" | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Release build output contains "BUILD SUCCEEDED"
    - `grep -rn "print(" HabitLand/ --include="*.swift" | grep -v "//" | wc -l` returns 0
    - `grep -rn "fatalError" HabitLand/ --include="*.swift" | wc -l` returns 0
    - All existing unit tests pass
  </acceptance_criteria>
  <done>Release build succeeds. Zero print() statements. Zero fatalError() calls. All screenshotMode checks inside #if DEBUG. All sheet sites have .hlSheetContent(). Test suite green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manual free tier QA and performance verification</name>
  <files></files>
  <action>
    Present the manual QA checklist to the user for free tier verification (D-08, D-09) and performance validation (D-16, D-17, D-18). All automated checks passed in Task 1. This checkpoint requires the user to manually walk through the app on a clean simulator and verify with Instruments.
  </action>
  <verify>User confirms all checklist items pass by typing "approved"</verify>
  <done>User has approved the free tier experience and performance metrics. App is ready for App Store submission.</done>
  <what-built>
    Plans 01 and 02 cleaned all debug artifacts, replaced logging, added premium sheet transitions, and fixed UI edge cases. The app is now production-ready and needs manual verification of the free tier experience and performance.
  </what-built>
  <how-to-verify>
    **Free Tier Testing (D-08, D-09) -- test on clean simulator:**

    1. Delete app from simulator, reset:
       - `xcrun simctl erase "iPhone 16"` (or manually delete app)
    2. Build and run in Debug mode (NOT screenshot mode -- no launch args)
    3. Walk through:
       - [ ] Onboarding completes smoothly
       - [ ] Can create 3 habits (free limit)
       - [ ] 4th habit triggers paywall
       - [ ] Analytics tab shows Pro gate
       - [ ] Challenge join shows Pro gate
       - [ ] Sleep insights Pro features gated
       - [ ] All tab bar destinations reachable
       - [ ] Settings rows all functional
       - [ ] Profile editable
       - [ ] Social tab without iCloud shows graceful message
       - [ ] Referral code entry works
       - [ ] Share sheet functional
       - [ ] Sheet transitions feel smooth and consistent (D-10)
       - [ ] Empty states look friendly (D-13)
       - [ ] Dark mode looks correct (D-14)
       - [ ] Long text doesn't break layouts (D-15)

    **Performance Verification (D-16, D-17, D-18, D-19):**

    4. Profile with Instruments (Release config):
       - Product > Profile in Xcode
       - [ ] Launch time < 1.5s (Time Profiler)
       - [ ] Scroll 60fps in habit list (Animation Hitches)
       - [ ] Memory < 100MB (Allocations)
       - [ ] No memory leaks (Leaks instrument)

    **Populated State (D-09):**

    5. Run with `-screenshotMode` flag to seed demo data
    6. Verify:
       - [ ] Dashboard displays habits and streaks correctly
       - [ ] Scroll performance good with multiple habits
       - [ ] Achievement badges render
  </how-to-verify>
  <acceptance_criteria>
    - User has tested all free tier flows listed above
    - User confirms performance within thresholds
    - User types "approved" to signal completion
  </acceptance_criteria>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
1. Release build succeeds clean
2. All automated audits pass (0 print, 0 fatalError, all screenshotMode guarded)
3. Human confirms free tier works end-to-end
4. Human confirms performance within thresholds
</verification>

<success_criteria>
- Release build clean with zero debug artifacts leaking
- Free tier experience verified on clean device: limits enforced, gates trigger, all screens reachable
- Performance within thresholds: <1.5s launch, 60fps scroll, <100MB memory
- Human approval received for App Store submission readiness
</success_criteria>

<output>
After completion, create `.planning/phases/04-quality-hardening-launch/04-03-SUMMARY.md`
</output>
