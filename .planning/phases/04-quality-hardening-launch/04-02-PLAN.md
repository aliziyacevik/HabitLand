---
phase: 04-quality-hardening-launch
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - HabitLand/DesignSystem/Effects.swift
  - HabitLand/Screens/Home/HomeDashboardView.swift
  - HabitLand/ContentView.swift
  - HabitLand/Screens/Settings/GeneralSettingsView.swift
  - HabitLand/Screens/Premium/PremiumGateView.swift
  - HabitLand/Screens/Premium/PaywallView.swift
  - HabitLand/Screens/Habits/HabitListView.swift
  - HabitLand/Screens/Habits/HabitDetailView.swift
  - HabitLand/Screens/Habits/CreateHabitView.swift
  - HabitLand/Screens/Onboarding/OnboardingView.swift
  - HabitLand/Screens/Social/SocialHubView.swift
  - HabitLand/Screens/Social/FriendProfileView.swift
  - HabitLand/Screens/Social/FriendsListView.swift
  - HabitLand/Screens/Social/SharedChallengesView.swift
  - HabitLand/Screens/Gamification/AchievementsView.swift
  - HabitLand/Screens/Settings/PrivacySettingsView.swift
  - HabitLand/Screens/Settings/DataExportView.swift
  - HabitLand/Screens/Settings/AppearanceSettingsView.swift
  - HabitLand/Screens/Analytics/MonthlyAnalyticsView.swift
  - HabitLand/Screens/Sleep/SleepDashboardView.swift
  - HabitLand/Screens/Notifications/NotificationCenterView.swift
autonomous: true
requirements:
  - QAL-05

must_haves:
  truths:
    - "Every sheet and fullScreenCover presentation has a smooth content entrance animation"
    - "Sheet transitions feel consistent across all screens -- same animation style everywhere"
    - "Empty states show friendly messages instead of blank screens"
    - "Long habit names truncate gracefully without layout breakage"
    - "Dark mode renders correctly across all screens"
  artifacts:
    - path: "HabitLand/DesignSystem/Effects.swift"
      provides: "HLSheetContent modifier and sheet animation presets"
      contains: "HLSheetContent"
    - path: "HabitLand/Screens/Home/HomeDashboardView.swift"
      provides: "Sheet presentations with .hlSheetContent()"
      contains: "hlSheetContent"
  key_links:
    - from: "All 20 files with .sheet()/.fullScreenCover()"
      to: "HabitLand/DesignSystem/Effects.swift"
      via: ".hlSheetContent() modifier on sheet content views"
      pattern: "\\.hlSheetContent\\(\\)"
---

<objective>
Add premium sheet transition animations and polish UI edge cases across all screens.

Purpose: Every sheet presentation should feel refined and consistent (per D-10, D-11, D-12), empty states should be friendly (D-13), dark mode should render correctly (D-14), and edge cases like long names should be handled (D-15).
Output: HLSheetContent modifier in Effects.swift + applied to all 32 sheet sites + edge case fixes.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-quality-hardening-launch/04-RESEARCH.md

<interfaces>
From HabitLand/DesignSystem/Effects.swift (existing animation presets):
```swift
// Line 56-70: Existing HLAnimation presets
static let microSpring = Animation.spring(response: 0.28, dampingFraction: 0.68)
static let gentleSpring = Animation.spring(response: 0.45, dampingFraction: 0.78)
static let fadeIn = Animation.easeOut(duration: 0.25)
static let slideIn = Animation.spring(response: 0.4, dampingFraction: 0.82)
static let progressFill = Animation.easeOut(duration: 0.8)
static let ringGlow = Animation.easeInOut(duration: 1.5).repeatForever(autoreverses: true)
static let celebration = Animation.spring(response: 0.35, dampingFraction: 0.55)
static let shimmerLoop = Animation.linear(duration: 1.8).repeatForever(autoreverses: false)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create HLSheetContent modifier and sheet animation presets in Effects.swift</name>
  <read_first>
    - HabitLand/DesignSystem/Effects.swift
  </read_first>
  <files>HabitLand/DesignSystem/Effects.swift</files>
  <action>
    Add to Effects.swift, after the existing HLAnimation presets (around line 70), per D-10/D-11/D-12:

    1. Add sheet animation presets to the existing animation section:
       ```swift
       // MARK: - Sheet Transitions
       static let sheetContentAppear = Animation.easeOut(duration: 0.3)
       ```

    2. Add the HLSheetContent ViewModifier (per research pattern):
       ```swift
       // MARK: - Sheet Content Modifier

       struct HLSheetContent: ViewModifier {
           @State private var appeared = false

           func body(content: Content) -> some View {
               content
                   .opacity(appeared ? 1 : 0)
                   .offset(y: appeared ? 0 : 8)
                   .animation(.spring(duration: 0.35, bounce: 0.0), value: appeared)
                   .onAppear {
                       DispatchQueue.main.asyncAfter(deadline: .now() + 0.05) {
                           appeared = true
                       }
                   }
           }
       }
       ```

    3. Add View extension for convenience:
       ```swift
       extension View {
           func hlSheetContent() -> some View {
               modifier(HLSheetContent())
           }
       }
       ```

    Style per D-11: subtle spring with no bounce (bounce: 0.0), opacity fade-in with gentle offset slide. Not flashy -- refined and consistent. Spring duration 0.35s is within the 0.35-0.45s range from research.
  </action>
  <verify>
    <automated>cd /Users/azc/works/HabitLand && grep -c "HLSheetContent" HabitLand/DesignSystem/Effects.swift</automated>
  </verify>
  <acceptance_criteria>
    - `grep "struct HLSheetContent: ViewModifier" HabitLand/DesignSystem/Effects.swift` returns a match
    - `grep "func hlSheetContent" HabitLand/DesignSystem/Effects.swift` returns a match
    - `grep "bounce: 0.0" HabitLand/DesignSystem/Effects.swift` returns a match
    - `grep "opacity.*appeared" HabitLand/DesignSystem/Effects.swift` returns a match
    - `grep "offset.*appeared" HabitLand/DesignSystem/Effects.swift` returns a match
  </acceptance_criteria>
  <done>HLSheetContent modifier exists in Effects.swift with subtle spring animation (duration 0.35, bounce 0.0), opacity fade-in, and gentle 8pt offset slide.</done>
</task>

<task type="auto">
  <name>Task 2: Apply .hlSheetContent() to all 32 sheet/fullScreenCover sites and fix UI edge cases</name>
  <read_first>
    - HabitLand/DesignSystem/Effects.swift
    - HabitLand/Screens/Home/HomeDashboardView.swift
    - HabitLand/ContentView.swift
    - HabitLand/Screens/Settings/GeneralSettingsView.swift
    - HabitLand/Screens/Premium/PremiumGateView.swift
    - HabitLand/Screens/Premium/PaywallView.swift
    - HabitLand/Screens/Habits/HabitListView.swift
    - HabitLand/Screens/Habits/HabitDetailView.swift
    - HabitLand/Screens/Habits/CreateHabitView.swift
    - HabitLand/Screens/Onboarding/OnboardingView.swift
    - HabitLand/Screens/Social/SocialHubView.swift
    - HabitLand/Screens/Social/FriendProfileView.swift
    - HabitLand/Screens/Social/FriendsListView.swift
    - HabitLand/Screens/Social/SharedChallengesView.swift
    - HabitLand/Screens/Gamification/AchievementsView.swift
    - HabitLand/Screens/Settings/PrivacySettingsView.swift
    - HabitLand/Screens/Settings/DataExportView.swift
    - HabitLand/Screens/Settings/AppearanceSettingsView.swift
    - HabitLand/Screens/Analytics/MonthlyAnalyticsView.swift
    - HabitLand/Screens/Sleep/SleepDashboardView.swift
    - HabitLand/Screens/Notifications/NotificationCenterView.swift
  </read_first>
  <files>
    HabitLand/Screens/Home/HomeDashboardView.swift,
    HabitLand/ContentView.swift,
    HabitLand/Screens/Settings/GeneralSettingsView.swift,
    HabitLand/Screens/Premium/PremiumGateView.swift,
    HabitLand/Screens/Premium/PaywallView.swift,
    HabitLand/Screens/Habits/HabitListView.swift,
    HabitLand/Screens/Habits/HabitDetailView.swift,
    HabitLand/Screens/Habits/CreateHabitView.swift,
    HabitLand/Screens/Onboarding/OnboardingView.swift,
    HabitLand/Screens/Social/SocialHubView.swift,
    HabitLand/Screens/Social/FriendProfileView.swift,
    HabitLand/Screens/Social/FriendsListView.swift,
    HabitLand/Screens/Social/SharedChallengesView.swift,
    HabitLand/Screens/Gamification/AchievementsView.swift,
    HabitLand/Screens/Settings/PrivacySettingsView.swift,
    HabitLand/Screens/Settings/DataExportView.swift,
    HabitLand/Screens/Settings/AppearanceSettingsView.swift,
    HabitLand/Screens/Analytics/MonthlyAnalyticsView.swift,
    HabitLand/Screens/Sleep/SleepDashboardView.swift,
    HabitLand/Screens/Notifications/NotificationCenterView.swift
  </files>
  <action>
    **Part A: Apply .hlSheetContent() to all sheet/fullScreenCover presentations (per D-10, Pitfall 4)**

    For EVERY `.sheet(isPresented:) { ... }` and `.fullScreenCover(isPresented:) { ... }` closure in all 20 files, add `.hlSheetContent()` to the root view inside the closure.

    Pattern:
    ```swift
    // Before:
    .sheet(isPresented: $showSomething) {
        SomeView()
    }

    // After:
    .sheet(isPresented: $showSomething) {
        SomeView()
            .hlSheetContent()
    }
    ```

    Same pattern for `.fullScreenCover()`. Also apply to `.sheet(item:)` variant closures.

    Known locations (32 total across 20 files):
    - HomeDashboardView.swift: 5 sheets
    - GeneralSettingsView.swift: 4 sheets
    - ContentView.swift: 2 sheets
    - PaywallView.swift: 2 sheets
    - PremiumGateView.swift: 2 sheets
    - HabitListView.swift: 2 sheets
    - OnboardingView.swift: 1 fullScreenCover + 1 sheet
    - Remaining 12 files: 1 each

    Grep all files for `.sheet(` and `.fullScreenCover(` to confirm every site is covered. Do NOT miss any.

    **Part B: Edge case fixes (per D-13, D-15)**

    While modifying each file, also fix these edge cases:

    1. **Long habit names (D-15):** In HabitListView.swift and HabitDetailView.swift, ensure habit name Text views have `.lineLimit(2)` and `.truncationMode(.tail)` where they could overflow.

    2. **Empty states (D-13):** Review each screen and ensure:
       - HabitListView: 0 habits shows a friendly "Start your first habit" message with an icon
       - SleepDashboardView: 0 sleep logs shows encouraging message to log first night
       - SocialHubView: Already handles iCloud unavailable -- verify it looks good
       - AchievementsView: Verify empty locked achievements don't look broken

    3. **Dark mode (D-14):** While touching each file, verify Color references use design system tokens (Color.hlPrimary, Color.hlBackground, etc.) not hardcoded colors. Fix any hardcoded white/black that would break in dark mode.

    IMPORTANT: Do NOT over-engineer empty states. Simple text with an SF Symbol icon is sufficient per Claude's discretion. No custom illustrations needed.
  </action>
  <verify>
    <automated>cd /Users/azc/works/HabitLand && grep -rn "\.sheet\(\|\.fullScreenCover(" HabitLand/ --include="*.swift" | wc -l && echo "---" && grep -rn "hlSheetContent" HabitLand/ --include="*.swift" | grep -v "Effects.swift" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - The count of `.hlSheetContent()` usages (excluding Effects.swift) equals the count of `.sheet(`/`.fullScreenCover(` sites (32)
    - `grep -rn "hlSheetContent" HabitLand/Screens/Home/HomeDashboardView.swift` returns 5 matches
    - `grep -rn "hlSheetContent" HabitLand/Screens/Settings/GeneralSettingsView.swift` returns 4 matches
    - `grep -rn "hlSheetContent" HabitLand/ContentView.swift` returns 2 matches
    - `grep -rn "lineLimit\|truncationMode" HabitLand/Screens/Habits/HabitListView.swift` returns at least 1 match
    - Build succeeds: `xcodebuild build -scheme HabitLand -destination 'platform=iOS Simulator,name=iPhone 16' 2>&1 | tail -3`
  </acceptance_criteria>
  <done>All 32 sheet/fullScreenCover presentations have .hlSheetContent() applied. Long habit names truncate gracefully. Empty states show friendly messages. No hardcoded colors breaking dark mode.</done>
</task>

</tasks>

<verification>
1. `grep -rn "hlSheetContent" HabitLand/ --include="*.swift" | wc -l` matches sheet count + definition count
2. `xcodebuild build -scheme HabitLand -destination 'platform=iOS Simulator,name=iPhone 16'` succeeds
3. Visual: sheets animate in with subtle fade + offset, no bounce, consistent across all screens
</verification>

<success_criteria>
- HLSheetContent modifier exists with refined spring animation (0.35s, no bounce)
- All 32 sheet/fullScreenCover sites use .hlSheetContent()
- Long habit names truncate with lineLimit
- Empty states have friendly messages
- Dark mode compatibility verified (no hardcoded colors)
- Build compiles clean
</success_criteria>

<output>
After completion, create `.planning/phases/04-quality-hardening-launch/04-02-SUMMARY.md`
</output>
